<h1>Burgers</h1>

<h3>Create burgers and devour them if you want.</h3>

<ul>
    {{#each uneaten_burgers}}
    <li>
        <p><strong>Burger: </strong>{{this.burger}}</p>
    </li>
    {{/each}}
</ul>

<ul>
    {{#each eaten_burgers}}
    <li>
        <p><strong>Burger: </strong>{{this.burger}}</p>
    </li>
    {{/each}}
</ul>

<form action="/" method="POST">
    <textarea type="text" name="burger"></textarea>

    <button type="submit">Submit</button>
</form>